server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ddubson/spring-cloud-config-poc
          basedir: target/config
  application:
    name: config-server